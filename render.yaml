services:
  - type: worker
    name: kurs-bot
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TOKEN
        sync: false
      - key: YUBORISH_ORALIGI
        value: "3600"
